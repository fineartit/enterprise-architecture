[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "enterprise-architecture"
authors = [{name = "Advisor", email = "fineartit@outlook.com"}]
dynamic = ["version", "description"]
